# Copy to `.env` in this folder (see `.gitignore` — `.env` is not committed).
# Paystack dashboard → Settings → API Keys & Webhooks → Public key (pk_test_… or pk_live_…).
PAYSTACK_PUBLIC_KEY=pk_test_replace_me
